{
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "name": "derive-version",
  "description": "Calculate `patch` &ndash; the number of commits since the last change in `major.minor` version.",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
